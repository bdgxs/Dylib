// .devcontainer/devcontainer.json

{
    "name": "Theos Development Environment",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "workspaceFolder": "/workspace",
    "customizations": {
        "vscode": {
            "extensions": [
                // Add any VS Code extensions you want to pre-install here
            ]
        }
    },
    "postCreateCommand": "echo 'Theos environment ready! Run \"make package\" to build your tweak.'"
}
